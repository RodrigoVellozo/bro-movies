import { ConvertMinToHoursPipe } from './convert-min-to-hours.pipe';

describe('ConvertMinToHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMinToHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
